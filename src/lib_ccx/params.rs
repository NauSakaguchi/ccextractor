use crate::lib_ccx::ccx_common_option::ccx_s_options;

impl ccx_s_options {
    pub fn parse_parameters(&mut self, _command_args:Vec<String>) -> i32
    {
        // unimplemented!();
        //need to implement later (see the documentation)

        9 // just return temp value
    }
}
